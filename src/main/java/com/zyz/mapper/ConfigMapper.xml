<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyz.mapper.ConfigMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zyz.pojo.Config">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="url" property="url" />
        <result column="create_time" property="createTime" />
        <result column="tags" property="tags" />
        <result column="type" property="type" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, url, create_time, tags, type
    </sql>
    <update id="updateAllIPData" parameterType="string">
        UPDATE task SET url = REPLACE(url, ori_ip, #{ip});
        UPDATE task SET ori_ip = #{ip};
        UPDATE generate_records SET url = REPLACE(url, ori_ip, #{ip});
        UPDATE generate_records SET reference = REPLACE(reference, ori_ip, #{ip});
        UPDATE generate_records SET ori_ip = #{ip};
        UPDATE task_records SET task_list = REPLACE(task_list, ori_ip, #{ip});
        UPDATE task_records SET ori_ip = #{ip};
    </update>

</mapper>
